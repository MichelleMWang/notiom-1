import { Button, Image } from "@chakra-ui/react";

const CreateButton = () => {
    return(
        <Button variant="createbutton">
            Create
        </Button>
    );
}
export default CreateButton; 